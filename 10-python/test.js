var JSEncrypt = require("./jsencrypt.js");

// function jsencrypt(publicKey, pwd) {
//   const encrypt = new JSEncrypt();
//   console.log(encrypt);
//   encrypt.setPublicKey(publicKey);
//   const encrypted = encrypt.encrypt(pwd);
//   return encrypted;
// }

function add(x, y) {
  return x + y;
}

console.log(add(1, 2));
